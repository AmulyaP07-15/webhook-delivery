[project]
name = "webhook-receiver"
version = "0.1.0"
description = "Verify webhooks delivered by the webhook-delivery service"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["webhook_receiver"]
